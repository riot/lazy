<component-with-slot-wrapper>
  <component-with-slot>
    <p>{props.greeting}</p>
  </component-with-slot>

  <script>
    import Loader from './loader.riot'
    import ComponentWithSlot from './component-with-slot.riot'
    import lazy from '../../index.next.js'

    export default {
      components: {
        ComponentWithSlot: lazy(Loader, () => Promise.resolve(ComponentWithSlot))
      }
    }
  </script>
</component-with-slot-wrapper>
